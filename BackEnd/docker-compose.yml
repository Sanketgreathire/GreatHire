## docker-compose disabled
# Docker compose removed per request. The original file has been preserved
# as `docker-compose.yml.disabled` in the same directory if you need to
# restore Kafka/Redis containers later.
